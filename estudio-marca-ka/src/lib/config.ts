// Configurações gerais do app.

// MODO TESTE: enquanto o sistema está em avaliação, todas as áreas ficam
// abertas — nada exige e-mail/senha (nem o estúdio, nem o painel admin, nem
// as abas de gestão). Para RELIGAR o login/gate, troque para `false`.
export const ACESSO_ABERTO: boolean = true
