# trigger: decodificador google news
name: Backfill + deploy (one-time)

# Recoleta os últimos dias usando o mapa ampliado de portais (BR/EUA/Europa),
# mescla por data de publicação e republica. Dispara quando este arquivo muda.
on:
  push:
    paths:
      - '.github/workflows/backfill-now.yml'

permissions:
  contents: write

jobs:
  backfill:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Instalar dependências
        run: pip install -r requirements.txt

      - name: Coletar últimos 4 dias com os novos portais
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SISTEMAKA_REPO: ${{ github.repository }}
        run: python -m sistemaka run --days 4

      - name: Salvar histórico no repositório
        run: |
          git config user.name "SistemaKA Bot"
          git config user.email "actions@github.com"
          git add data
          git commit -m "data: coleta com mapa ampliado de portais (BR/EUA/Europa)" || echo "sem mudanças"
          git push || echo "nada a enviar"

      - name: Publicar no Netlify
        run: |
          npx -y @netlify/mcp@latest \
            --site-id 08fc9134-351a-4237-9891-fd4dea56d013 \
            --proxy-path "https://netlify-mcp.netlify.app/proxy/eyJhbGciOiJkaXIiLCJlbmMiOiJBMjU2R0NNIn0..ouyNsiVwmH9747vU.g3sF0fumEnSK0_VejvF1VYXVEaEMf1GAJteILLMgePvJ2lGMRoH71wcTWgSAV_bvcif37Ok6EtaMO9W2K8cHr21MfajZkvmLPBhEzsYjmV3cfMt2cFBneHoNCSBApgCew4Nfl_2F37g0Ue2qR5h5yGG4LJQkAFnEc7tvIuJoHGZO6zzKHWw6xfdWJ_MBH950I0-0-SDUqUQROr9FHZqrsZsUqkusEhdmh6SEgCbSH-Or_LO97cYeDjd9Njq8npDvM8woVeMTC6FlIolUnpaoCLD4w0d8oKtTHSNNbCaP2rLvWpAPjeWFp_D1UKgrhNf5TrevbDc3cNPeFhqsUJtjngXSRT2Vu36i8jqGRswT7zseZq8BQ6JWiFdPGTXJ_w7WABh3hlKk.PgtDUy5N-lbDrnSmUZRUCw"
