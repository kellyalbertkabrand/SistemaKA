name: Backfill + deploy (one-time)

# Recoleta os últimos dias usando o mapa ampliado de portais (BR/EUA/Europa),
# mescla por data de publicação e republica. Dispara quando este arquivo muda.
on:
  push:
    paths:
      - '.github/workflows/backfill-now.yml'

permissions:
  contents: write

jobs:
  backfill:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Instalar dependências
        run: pip install -r requirements.txt

      - name: Coletar últimos 4 dias com os novos portais
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SISTEMAKA_REPO: ${{ github.repository }}
        run: python -m sistemaka run --days 4

      - name: Salvar histórico no repositório
        run: |
          git config user.name "SistemaKA Bot"
          git config user.email "actions@github.com"
          git add data
          git commit -m "data: coleta com mapa ampliado de portais (BR/EUA/Europa)" || echo "sem mudanças"
          git push || echo "nada a enviar"

      - name: Publicar no Netlify
        run: |
          npx -y @netlify/mcp@latest \
            --site-id 08fc9134-351a-4237-9891-fd4dea56d013 \
            --proxy-path "https://netlify-mcp.netlify.app/proxy/eyJhbGciOiJkaXIiLCJlbmMiOiJBMjU2R0NNIn0..lHIZhkbht-23VcJS.gYHEcr44LgRxIZ_YHN26Smj_dIcu42VXMOWNSkEjLxtN-OWn-z8yJtGEccAUGX1wp0FZS3ctGXKk1olItUNlvA5D1vxbRIqTCeOdj36-_kGNisNYVi-qXwjdRWqnTCnhLIa2OgCHVH2Qjt0PDxMyPrKBpPPR-5aQnsSpXn7xKek57Ii8K0CkLlw2DcuJVy8Pya6qv2IXQOhAdHjBLA2GW0u7Rv_GY_mGI58Uz4kLM1tCOqxuRuG3POz6cvAwz1hoPW_p8zPfQP7yHuSC8zixGEMWp4HWl_gXKXovkYwyODV_NtDmW-DU1l1CPFJfZaNK5GfsEWOPaeNZGIpk4QXRyIfO3ocWjHSdm6G4vra3O9wVIR1p3xzSnuuWImWe6neBCSYBCb_k.23evOR9Ao2aijYHj9aY2gA"
