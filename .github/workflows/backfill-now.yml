name: Backfill + deploy (one-time)

# Recoleta os últimos dias usando o mapa ampliado de portais (BR/EUA/Europa),
# mescla por data de publicação e republica. Dispara quando este arquivo muda.
on:
  push:
    paths:
      - '.github/workflows/backfill-now.yml'

permissions:
  contents: write

jobs:
  backfill:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Instalar dependências
        run: pip install -r requirements.txt

      - name: Coletar últimos 4 dias com os novos portais
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          SISTEMAKA_REPO: ${{ github.repository }}
        run: python -m sistemaka run --days 4

      - name: Salvar histórico no repositório
        run: |
          git config user.name "SistemaKA Bot"
          git config user.email "actions@github.com"
          git add data
          git commit -m "data: coleta com mapa ampliado de portais (BR/EUA/Europa)" || echo "sem mudanças"
          git push || echo "nada a enviar"

      - name: Publicar no Netlify
        run: |
          npx -y @netlify/mcp@latest \
            --site-id 08fc9134-351a-4237-9891-fd4dea56d013 \
            --proxy-path "https://netlify-mcp.netlify.app/proxy/eyJhbGciOiJkaXIiLCJlbmMiOiJBMjU2R0NNIn0..FGiNGAQXs9swq-aM.noZdCvjWFUk1l8fm2-wa-NYxgc6yxOTRMZy0SG-tpZo-JdRuEzswwfEqE8_OS7NPx26Fam55JGAx30hcpFYRr83XivL2J7ycwxaTFqJ6sKE9x6_DcakbSMnOYofZzWPHbCRWkX8K69dnbBz8shxS2-JtKUMX_qy-zUYr00x7dllcs481uTeCihbC8K_Pg_OgGOGVVF421LtugYbboJhEhmkCUBUWW8K_mWjKlKOKeX0COPDi4ok-vqpKsT2BzNFI-gEEjHTrMOP6hx6DfiNNe_P4GX6TYw6WEptDY6m54B4F2lgLjdm_F-m8_3y7Z6olV5l3vqTVj8-wyH3_sYGNTGWckyB3nQyGLZvHfCx9iXLlLSsfKoe1i3HZhNDtVypqeN63dC3x.wTawm5z0X8tFHleri5Aynw"
